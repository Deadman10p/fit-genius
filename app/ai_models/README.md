# AI Model Uploads

Drop your fine-tuned `.guuf` file here for direct connection in future AI model loading.

- Upload path: `app/ai_models/`
- Default filename: `model.guuf`
- Configure with `AI_MODEL_UPLOAD_DIR` and `AI_MODEL_FILE_NAME` in `.env`

This folder is intended for future direct integration of a fine-tuned AI model.
